apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop
  region: us-east-1

managedNodeGroups:
  - name: managed
    instanceType: t3.micro
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
