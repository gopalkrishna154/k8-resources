apiVersion: v1
kind: Service
metadata:
  name: nginx-np
  # service labels
  labels:
    project: roboshop122
    environment: dev
spec:
  type: LoadBalancer
#service uses the below lables to select the pods

  selector:
    project: roboshop122
    component: frontend
    tier: frontend
  ports:
    - protocol: TCP
      port: 80 # service port
      targetPort: 80 # cotainer port
      nodeport: 30007