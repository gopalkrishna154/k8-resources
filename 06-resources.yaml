apiVersion: v1
kind: Pod
metadata:
  name: resources
spec:
  containers:
  - name: nginx
    image: nginx:latest 
    resources:
      requests: # soft limit
        cpu: "100m"  # 100m is 1 cpu
        memory:  "128Mi"
      limits:  # hard limit
        cpu: "150m"  
        memory:  "256Mi"